[Excel]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
;starttags = taglist
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
excludetags = WorksheetOptions
;-- startstoring: start storing when the "Section" column contains this string
startstoring = 1
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = Row
;-- sectionprefix: String prefix to use in section numbering
;sectionprefix = list
;-- datatags: list of tag names that start a new data column
datatags = Cell
;-- titletag: XML tag used to mark the testplan section title
;titletag = taglist
;-- descriptiontag: XML tag used to mark the description text
;descriptiontag = taglist
;-- goaltag: XML tag used to mark the goal of a section
;goaltag = taglist
;-- weighttag: XML tag used to mark the weight of a section
;weighttag = taglist
;-- linktag: XML tag used to mark the cover item (link) name
;linktag = taglist
;-- linkattr: XML attribute name which contains the cover item (link) path
;linkattr = string
;-- typeattr: XML attribute name which contains the cover item (link) type
;typeattr = string
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal,AtLeast,LinkWeight,Unimplemented,LinkExclusion,LinkExclusionComment
datafields = Section,Title,Description,Link,Type,Weight,Goal
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH,ATLEAST,LINKWEIGHT,UNIMPLEMENTED,LINKEXCLUSION,LINKEXCLUSIONCOMMENT
;datalabels = list
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 0
;-- startsection: initial section number for auto-numbering
;startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
stylesheet = excel.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
[Word]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
starttags = body
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
excludetags = binData,delText
;-- startstoring: start storing when the "Section" column contains this string
;startstoring = string
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = sub-section
;-- sectionprefix: String prefix to use in section numbering
;sectionprefix = list
;-- datatags: list of tag names that start a new data column
datatags = p
;-- titletag: XML tag used to mark the testplan section title
;titletag = taglist
;-- descriptiontag: XML tag used to mark the description text
;descriptiontag = taglist
;-- goaltag: XML tag used to mark the goal of a section
;goaltag = taglist
;-- weighttag: XML tag used to mark the weight of a section
;weighttag = taglist
;-- linktag: XML tag used to mark the cover item (link) name
;linktag = taglist
;-- linkattr: XML attribute name which contains the cover item (link) path
;linkattr = string
;-- typeattr: XML attribute name which contains the cover item (link) type
;typeattr = string
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal,AtLeast,LinkWeight,Unimplemented,LinkExclusion,LinkExclusionComment
datafields = Title
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH,ATLEAST,LINKWEIGHT,UNIMPLEMENTED,LINKEXCLUSION,LINKEXCLUSIONCOMMENT
;datalabels = list
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 1
;-- startsection: initial section number for auto-numbering
startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
;stylesheet = filename.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
[DocBook]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
starttags = book,article
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
;excludetags = taglist
;-- startstoring: start storing when the "Section" column contains this string
;startstoring = string
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = chapter,section
;-- sectionprefix: String prefix to use in section numbering
;sectionprefix = list
;-- datatags: list of tag names that start a new data column
datatags = para
;-- titletag: XML tag used to mark the testplan section title
titletag = title
;-- descriptiontag: XML tag used to mark the description text
;descriptiontag = taglist
;-- goaltag: XML tag used to mark the goal of a section
;goaltag = taglist
;-- weighttag: XML tag used to mark the weight of a section
;weighttag = taglist
;-- linktag: XML tag used to mark the cover item (link) name
;linktag = taglist
;-- linkattr: XML attribute name which contains the cover item (link) path
;linkattr = string
;-- typeattr: XML attribute name which contains the cover item (link) type
;typeattr = string
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal,AtLeast,LinkWeight,Unimplemented,LinkExclusion,LinkExclusionComment
;datafields = list
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH,ATLEAST,LINKWEIGHT,UNIMPLEMENTED,LINKEXCLUSION,LINKEXCLUSIONCOMMENT
;datalabels = list
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 1
;-- startsection: initial section number for auto-numbering
startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
;stylesheet = filename.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
[Frame]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
starttags = Document
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
;excludetags = taglist
;-- startstoring: start storing when the "Section" column contains this string
;startstoring = string
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = Chapter
;-- sectionprefix: String prefix to use in section numbering
;sectionprefix = list
;-- datatags: list of tag names that start a new data column
datatags = Paragraph
;-- titletag: XML tag used to mark the testplan section title
titletag = Title
;-- descriptiontag: XML tag used to mark the description text
;descriptiontag = taglist
;-- goaltag: XML tag used to mark the goal of a section
;goaltag = taglist
;-- weighttag: XML tag used to mark the weight of a section
;weighttag = taglist
;-- linktag: XML tag used to mark the cover item (link) name
;linktag = taglist
;-- linkattr: XML attribute name which contains the cover item (link) path
;linkattr = string
;-- typeattr: XML attribute name which contains the cover item (link) type
;typeattr = string
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal,AtLeast,LinkWeight,Unimplemented,LinkExclusion,LinkExclusionComment
;datafields = list
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH,ATLEAST,LINKWEIGHT,UNIMPLEMENTED,LINKEXCLUSION,LINKEXCLUSIONCOMMENT
;datalabels = list
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 1
;-- startsection: initial section number for auto-numbering
startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
;stylesheet = filename.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
[GamePlan]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
starttags = TESTPLAN
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
;excludetags = taglist
;-- startstoring: start storing when the "Section" column contains this string
;startstoring = string
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = PLAN,FEATURE,COVERAGE,PROPERTY,TESTCASE,GROUP,HLR
;-- sectionprefix: String prefix to use in section numbering
sectionprefix = PL,F,C,P,T,G,H
;-- datatags: list of tag names that start a new data column
datatags = UserData
;-- titletag: XML tag used to mark the testplan section title element
titletag = NAME
;-- descriptiontag: XML tag used to mark the description text element
descriptiontag = DESC
;-- goaltag: XML tag used to mark the goal element
goaltag = GOAL
;-- weighttag: XML tag used to mark the weight element
weighttag = WEIGHT
;-- linktag: XML tag used to mark the cover item (link) element
linktag = LINK
;-- typeattr: XML attribute which contains the cover item (link) type
typeattr = type
;-- linkattr: XML attribute which contains the cover item (link) path/name
;linkattr = string
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal,AtLeast,LinkWeight,Unimplemented,LinkExclusion,LinkExclusionComment
;datafields = list
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH,ATLEAST,LINKWEIGHT,UNIMPLEMENTED,LINKEXCLUSION,LINKEXCLUSIONCOMMENT
datalabels = RESPONSIBLE
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 1
;-- startsection: initial section number for auto-numbering
startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0 
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
stylesheet = gameplan.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
[MVCExcel]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
;starttags = taglist
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
excludetags = WorksheetOptions
;-- startstoring: start storing when the "Section" column contains this string
startstoring = 1
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = Row
;-- sectionprefix: String prefix to use in section numbering
;sectionprefix = list
;-- datatags: list of tag names that start a new data column
datatags = Cell
;-- titletag: XML tag used to mark the testplan section title
;titletag = taglist
;-- descriptiontag: XML tag used to mark the description text
;descriptiontag = taglist
;-- goaltag: XML tag used to mark the goal of a section
;goaltag = taglist
;-- weighttag: XML tag used to mark the weight of a section
;weighttag = taglist
;-- linktag: XML tag used to mark the cover item (link) name
;linktag = taglist
;-- linkattr: XML attribute name which contains the cover item (link) path
;linkattr = string
;-- typeattr: XML attribute name which contains the cover item (link) type
;typeattr = string
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal
datafields = Section,Title,Description,Link,Type,Path,Weight,Goal,Specification
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH
;datalabels = list
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 0
;-- startsection: initial section number for auto-numbering
;startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
stylesheet = excel.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
[QconnectBitwise]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
;starttags = taglist
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
excludetags = WorksheetOptions
;-- startstoring: start storing when the "Section" column contains this string
startstoring = 1
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = Row
;-- sectionprefix: String prefix to use in section numbering
;sectionprefix = list
;-- datatags: list of tag names that start a new data column
datatags = Cell
;-- titletag: XML tag used to mark the testplan section title
;titletag = taglist
;-- descriptiontag: XML tag used to mark the description text
;descriptiontag = taglist
;-- goaltag: XML tag used to mark the goal of a section
;goaltag = taglist
;-- weighttag: XML tag used to mark the weight of a section
;weighttag = taglist
;-- linktag: XML tag used to mark the cover item (link) name
;linktag = taglist
;-- linkattr: XML attribute name which contains the cover item (link) path
;linkattr = string
;-- typeattr: XML attribute name which contains the cover item (link) type
;typeattr = string
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal,AtLeast,LinkWeight,Unimplemented,LinkExclusion,LinkExclusionComment
datafields = Section,Title,Description,Link,Type,Weight,Goal
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH,ATLEAST,LINKWEIGHT,UNIMPLEMENTED,LINKEXCLUSION,LINKEXCLUSIONCOMMENT
;datalabels = list
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 0
;-- startsection: initial section number for auto-numbering
;startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
stylesheet = qconnect_bitwise.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
[QconnectNonBitwise]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
;starttags = taglist
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
excludetags = WorksheetOptions
;-- startstoring: start storing when the "Section" column contains this string
startstoring = 1
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = Row
;-- sectionprefix: String prefix to use in section numbering
;sectionprefix = list
;-- datatags: list of tag names that start a new data column
datatags = Cell
;-- titletag: XML tag used to mark the testplan section title
;titletag = taglist
;-- descriptiontag: XML tag used to mark the description text
;descriptiontag = taglist
;-- goaltag: XML tag used to mark the goal of a section
;goaltag = taglist
;-- weighttag: XML tag used to mark the weight of a section
;weighttag = taglist
;-- linktag: XML tag used to mark the cover item (link) name
;linktag = taglist
;-- linkattr: XML attribute name which contains the cover item (link) path
;linkattr = string
;-- typeattr: XML attribute name which contains the cover item (link) type
;typeattr = string
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal,AtLeast,LinkWeight,Unimplemented,LinkExclusion,LinkExclusionComment
datafields = Section,Title,Description,Link,Type,Weight,Goal
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH,ATLEAST,LINKWEIGHT,UNIMPLEMENTED,LINKEXCLUSION,LINKEXCLUSIONCOMMENT
;datalabels = list
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 0
;-- startsection: initial section number for auto-numbering
;startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
stylesheet = qconnect_nonbitwise.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
[Polarion]
;-- tagseparators: delimiters used between tag names in a list
tagseparators = ,
;-- starttags: XML tag regions to start the data extraction
starttags = TESTPLAN
;-- stoptags: XML tag regions to stop the data extraction
;stoptags = taglist
;-- excludetags: XML tag regions to skip during data extraction
;excludetags = taglist
;-- startstoring: start storing when the "Section" column contains this string
;startstoring = string
;-- sectiontags: list of tag names that start a new testplan section
sectiontags = SECTION
;-- sectionprefix: String prefix to use in section numbering
;sectionprefix = PL,F,C,P,T,G,H
;-- datatags: list of tag names that start a new data column
datatags = CUSTOMDATA
;-- titletag: XML tag used to mark the testplan section title element
titletag = TITLE
;-- descriptiontag: XML tag used to mark the description text element
descriptiontag = DESCRIPTION
;-- goaltag: XML tag used to mark the goal element
goaltag = GOAL
;-- weighttag: XML tag used to mark the weight element
weighttag = WEIGHT
;-- linktag: XML tag used to mark the cover item (link) element
linktag = LINK
;-- typeattr: XML attribute which contains the cover item (link) type
typeattr = type
;-- linkattr: XML attribute which contains the cover item (link) path/name
;linkattr = type
;-- datafields: list of data fields which appear in-order
;   Known names: Section,Title,Description,Link,Type,Weight,Goal,AtLeast,LinkWeight,Unimplemented,LinkExclusion,LinkExclusionComment
;datafields = PID, Specification, Priority
;-- datalabels: list of data fields which are explicitly labeled
;   Already built-in: DESCRIPTION,LINK,TYPE,WEIGHT,GOAL,PATH,ATLEAST,LINKWEIGHT,UNIMPLEMENTED,LINKEXCLUSION,LINKEXCLUSIONCOMMENT
;datalabels = PID,Specification,Priority
;-- autonumber: enable generation of section numbers based on hierarchy
autonumber = 1
;-- startsection: initial section number for auto-numbering
startsection = 1
;-- root: the section number for the top level (root) of the test plan
root = 0 
;-- title: the path name to use for the top level of the test plan
title = testplan
;-- stylesheet: name of an XSL stylesheet to apply to the incoming XML file
;stylesheet = gameplan.xsl
;-- searchpath: path(s) in which the xml2ucdb utility will locate the specified XML file(s)
;searchpath = define search paths here
